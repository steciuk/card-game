import { HEX_COLORS } from './phaserComponents/HexColors';

export const PHASER_CONFIG = {
	type: Phaser.AUTO,
	width: 800, //window.innerWidth,
	height: 800, //window.innerHeight,
	backgroundColor: HEX_COLORS.PINK,
};
